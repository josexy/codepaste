import { Outlet } from "react-router-dom";
import { GPanel101 } from "./GPanel";

export default function GPasteSearch() {
    return (
        <GPanel101>
            <Outlet />
        </GPanel101>
    )
}